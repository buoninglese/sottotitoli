# ═══════════════════════════════════════════════════════════
# Sottotitoli · robots.txt — rewritten 2026-09-23, revised 2026-09-24
# ═══════════════════════════════════════════════════════════
# Public pages are listed in sitemap.xml.
# Internal backend / docs / dev / voice sources no longer live
# in this repo (moved to the private backup).
# ═══════════════════════════════════════════════════════════

User-agent: *
Sitemap: https://www.sottotitoli.pro/sitemap.xml

# Not content — keep config out of the index
Disallow: /config.js
Disallow: /config.secrets.js
Disallow: /config.json

# Internal school/admin console. Unlinked from every page, and inert without a
# Google sign-in — every action is authorised server-side by RLS policies and
# is_platform_admin() checks. Fenced off so it stays out of search results.
# This is NOT a secret and must never be treated as one: the page protects
# nothing, by design. See the security-model comment in admin.html.
Disallow: /admin.html
